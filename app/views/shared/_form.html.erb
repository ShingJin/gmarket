<%= form_for(@comment) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div id="comment-text">
    <textarea id="comment_comment" name="comment[comment]" size="30" type="text" placeholder="Have something to say?"></textarea>
  </div>
    <%= f.hidden_field :professor_id, :value=>params[:id] %>
    <%= f.submit "Submit", :class=>"button white", :remote=>true%>

<% end %>
